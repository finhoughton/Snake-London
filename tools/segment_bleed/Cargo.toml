[package]
name = "segment_bleed"
version = "0.1.0"
edition = "2021"

[dependencies]
resvg = { version = "0.45", default-features = false }
rayon = "1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[profile.release]
opt-level = 3
